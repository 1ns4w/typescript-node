// Tuples enable defining an array length and its elements data types and position
let mixed: [number, string]
mixed = [1, 'a'];

// Arrays also support destructuring in TypeScript
const [firstElement] = mixed
console.log(firstElement)
